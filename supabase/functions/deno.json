{
  "//": "Marks supabase/functions as a Deno project so the Deno language server activates here (fixes 'Cannot find name Deno' in editors with the Deno extension). Does not affect the Vite build.",
  "lint": { "rules": { "tags": ["recommended"] } },
  "importMap": "./import_map.json"
}
